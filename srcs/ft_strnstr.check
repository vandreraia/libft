char *strnstr(const char *s1, const char *s2, size_t len)
{
	size_t i;
	int j:
	char *p;

	j = 0;
	i = 0;
	if (*s2 == '\0')
		return (s1);
	while (s1[i] != s2[j] && s1[i] && i < len)
		i++;
	*p = s1[i];
	while (s1[i] == s2[j])
	{
		i++;
		j++;
		if (s1[i] == '\0')	
			return (p);
		if (s1[i] != s2[j])
			return (0);
	}
	return (0);
}
